version: "3"

services:
  james-site:
    image: "james-site"
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - "./prisma/db:/app/db"
    environment:
      - DATABASE_URL=file:/app/db/docker.db

