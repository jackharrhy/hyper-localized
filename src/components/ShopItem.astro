---
const { id, title, image } = Astro.props;
---

<a
  href={`/shop/item/${id}`}
  class="w-1/4 flex flex-col justify-center items-center px-2 py-4 gap-2 m-2 border-2 border-transparent hover:border-primary-500 transition-all delay-100"
>
  <p class="text-2xl text-center font-bold underline">{title}</p>
  <img src={image} class="shrink-0 object-contain h-48 w-32" />
</a>
