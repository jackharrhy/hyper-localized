---
const { slug, title, image } = Astro.props;
---

<a href={`/videos/${slug}`}>
  <div
    class="flex justify-center items-center p-2 gap-4 m-2 border-2 border-transparent hover:border-primary-500 transition-all delay-100"
  >
    <img src={image} class="shrink-0 object-contain h-32 w-64" />
    <p class="text-2xl font-bold underline">{title}</p>
  </div>
</a>
